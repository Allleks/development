### Models
* Qt models tree и прочее как выделять ячейку(QModelIndex и производить операции с ней)
```cpp
QPair<QModelIndex, AbstractOperationsTreeViewItem*> OperationsWindow::getCurrentOperationsTreeItem(const QItemSelection& selectedItems) const
{
	try
	{
		auto selectionModel = operationsTreeView.selectionModel();
		if (selectionModel == nullptr)
{
	qCritical() << "SELECTIONMODEL IS NULL!";

	return QPair<QModelIndex, AbstractOperationsTreeViewItem*>();
	return QPair<QModelIndex, AbstractOperationsTreeViewItem*>(
	selectedIndexes.first(), 
	static_cast<AbstractOperationsTreeViewItem*>(selectedIndexes.first().internalPointer()));
}
```
Что означает Qt::UserRole? и как его использовать
```cpp
auto currentComponentId = componentVariations->at(item->data(Qt::UserRole).toInt())->GetComponentType()->GetId();
```
___
### Работа с событиями в qt 
```cpp
void closeEvent(QCloseEvent* event) override;

event->ignore();
event->accept();

emit onClose(true/false);
```
___
### QObjec когда используется? такая запись
```cpp
QObject::connect(
```
___
### Совет от бывалых про сигналы и слоты
Соединение всех слотов и сигналов надо выполнять в **главном окне,** так как именно оно знает всё о формах, которые в нём используются.
___
# Реализовать сериализацию/десериализацию данных ????
___
**QStrintList** - 
```cpp
QStringList headers = {"Код", "Название", "Описание", "Ед.изм", "Мин. знач", "Макс.знач", "Тест. знач"};
```
___
### **Слоты без параметров** - напрямую
```cpp
// Если метод объявлен как слот и не принимает параметров
connect(button, &QPushButton::clicked, this, &MyClass::someSlot);
```
### **Методы с параметрами или обычные функции** - через лямбду
```cpp
// Best practice для вашего случая
connect(ui.platformHeightExpressionEdit_button, &QPushButton::clicked,
        this, [this]() {
    // Лямбда захватывает this и может обращаться к UI
    QString text = ui.platformHeightExpression_label->text();
    showEditExpressionWindow(text);  // Вызов метода с параметром
});
```
**Вывод:** Для вызова методов с параметрами или любой сложной логики обработки используйте лямбды - это современный и рекомендуемый подход в Qt.
___
## sourcegit 
не смог сделать слияние в старую  ветку(ошибка), решил вопрос через bash консоль git так все ок
___
## Работаем с выделенным содержимым ячеек таблицы
```cpp
//метод selected проходимся по выделенным ячейкам
for (const auto item : ui.componentVariation_table->selectedItems())
{
//проверка если столбец есть идем дальше
	if (item->column() != 0)
	{
		continue;
	}
	//вытаскиваем содержимое ячейки черех data и роль переводим в нужный тип
	auto componentVariationId = item->data(Qt::UserRole).toInt();
	//и дальше логика с полученными данными	
}
```
___
## В VisualStudio подключаем хедер ui формы
```cpp
//смотрим OutpulFile в настройках проекта
form.ui -> #include "ui_form_window.h"
```
___
## Модальность окна в ui design
3 вида модальности, на проекте 3 вариант use
```cpp
windowModality
{
	NonModal,
	WindowModal,
	ApplicationModal  <-----
}
```
___
## QTextEdit
возврат содержимого
```cpp
QString inputExpressionText = ui.expressionInput_textEdit->toPlainText();
```
___
## Про типы данных QString и 
- `QString` хранит данные в UTF-16 (в Windows), а `std::string_view` ожидает UTF-8
- Нужно явное преобразование через `toUtf8()` или `toStdString()`
___
## Механизм поиска
```cpp
connect(ui.searchSparePart_lineEdit, &QLineEdit::editingFinished, this, &HardcodedSparePartWindow::findSpareParts);
```
___
### **Особенности видимости и объявление сигналов/слотов**
Сигналы и слоты в Qt имеют глобальную видимость в пределах одного `QObject` дерева. Они не требуют соединения только в главном окне
```cpp
// Окно 1 (FormA.cpp) - может соединить свой сигнал со слотом Окна 2
connect(this, &FormA::mySignal, formB, &FormB::handleSignal);

// Окно 2 (FormB.cpp) - может соединить свой сигнал со слотом Окна 3
connect(this, &FormB::anotherSignal, formC, &FormC::processData);
```
**Вывод:** Соединения можно и нужно делать в наиболее подходящем месте с точки зрения архитектуры, а не только в главном окне.
**Ключевой принцип:** Соединяйте объекты в том месте, где у вас есть естественный доступ к обоим участникам соединения. Часто это место создания объектов или инициализации модуля.
## **📊 ТАБЛИЦА ВОЗМОЖНОСТЕЙ:**

|Где создаем connect|Доступ к источнику|Доступ к приемнику|Пример|
|---|---|---|---|
|В классе-источнике|✅ `this`|✅ Через параметр|`connect(this, &A::sig, target, &B::slot)`|
|В классе-приемнике|✅ Через параметр|✅ `this`|`connect(source, &A::sig, this, &B::slot)`|
|В родительском классе|✅ Члены класса|✅ Члены класса|В `MainWindow` соединяет своих детей|
|В отдельном сервисе|✅ Переданы как параметры|✅ Переданы как параметры|`Service::connect(a, b)`|
|Глобально|✅ Через синглтон|✅ Через синглтон|`Registry::connectAll()`|

## **🚨 ЧЕГО НЕ ДЕЛАТЬ:**
```cpp
// ❌ ПЛОХО: Соединения разбросаны по всему коду
void func1() { connect(a, b); }
void func2() { connect(c, d); }

// ❌ ОПАСНО: Нет проверки существования объектов
connect(pointerThatMayBeNull, &Class::signal, receiver, &Class::slot);

// ✅ ХОРОШО: Централизованное управление соединениями
class ConnectionManager {
    void setupAllConnections() {
        connect(a, b);
        connect(c, d);
        connect(e, f);
    }
};
```
___
## Исключения
Какие хорошие практики нужны?
```cpp
try
{

}
catch (const std::exception& ex)
{
//что вернет исключение?
	qCritical() << ex.what();
}
```
___
## Диалоговое окно QBoxMessage
может быть в любом месте по условию?
```cpp
if (expressionResult.isEmpty()) 
{
	QMessageBox::warning(this, "Ошибка", "Введите выражение");
	return;
}
```
___
## Указатели - 💀 **АНТИПАТТЕРНЫ**
#anti_patterns
```cpp
// ❌ АНТИПАТТЕРН: Доверчивый код
void dangerous(Data* data) {
    data->process();  // Надеемся, что data не nullptr
}

// ❌ АНТИПАТТЕРН: Ручное управление
Data* data = new Data();
// ... 100 строк кода ...
delete data;  // Забудем - получим утечку

// ❌ АНТИПАТТЕРН: Сырые указатели в контейнерах
std::vector<Data*> items;  // Кто удаляет?

// ✅ ПРАВИЛЬНО
std::vector<std::unique_ptr<Data>> items;  // Автоматическое управление
```
**Запомни:** В продакшене каждый неинициализированный указатель - это мина замедленного действия. Каждая проверка `if (ptr)` - это спасенная жизнь твоего приложения!
___
## **Почему нельзя передавать ссылки в лямбды для connect:**
**1. Время жизни объектов:**
```cpp
{
    QLabel localLabel("text");  // локальная переменная
    setupExpressionButton(button, localLabel, edit); // передали ссылку
    
} // localLabel уничтожается здесь!

// А лямбда всё ещё хранит ссылку на уничтоженный объект!
// CRASH при вызове!
```
**2. Лямбда захватывает ссылки, а не значения:**
```cpp
QLabel* label = new QLabel("text");
connect(button, &QPushButton::clicked, this, [this, &label]() {
    // &label - это ссылка на указатель!
    // Если label удалится, ссылка станет невалидной
    qDebug() << label->text();  // возможный креш
});

delete label;  // label удалён
// Лямбда всё ещё имеет ссылку на удалённую память
```

**📊 РАЗНИЦА МЕЖДУ ЗАХВАТОМ ПО ЗНАЧЕНИЮ И ПО ССЫЛКЕ:**
```cpp
QLabel* label = new QLabel("Hello");

// Захват по значению (копируется указатель)
auto lambda1 = [label]() { /* имеет свою копию указателя */ };

// Захват по ссылке (ссылка на указатель)
auto lambda2 = [&label]() { /* ссылается на внешнюю переменную label */ };

delete label;

// lambda1: имеет указатель на удалённую память (плохо, но понятно)
// lambda2: имеет ссылку на переменную, которая может измениться (очень плохо)
```
 **🎯 ПРАВИЛА ДЛЯ CONNECT И ЛЯМБД:**
1. **Никогда не захватывайте по ссылке** объекты, которые могут быть удалены
2. **Для QObject используйте QPointer** или захват по значению с проверками
3. **Для простых данных (QString, int)** захватывайте по значению (копия)
4. **Проверяйте указатели** перед использованием в лямбде
```cpp
// ❌ Ссылки в захвате
[&label, &edit]() { ... }

// ❌ Сырые указатели без проверок
[label, edit]() { edit->setText(...); }

// ❌ Захват this по ссылке (бесполезно и опасно)
[&this]() { ... }  // syntax error, но если бы работало - опасно

// ✅ Правильно
[this, label, edit]() { if (label && edit) ... }
[this, title = label->text(), editName = edit->objectName()]() { ... }
```
**Вывод:** Передавайте по значению (указатели) с проверками или используйте QPointer. Ссылки в лямбдах для connect — источник ошибок и крешей.
___
## Находим ui элементы на форме
#ui
```cpp
auto targetEdit = findChild<QTextEdit*>(editObjectName);
```
___
## Шаблонный класс
#template
* Ключевое слово `final` запрещает дальнейшее наследование от этого класса
* `Result` публично наследует от интерфейса `IResult<T>`
- `IResult<T>` также является шаблоном с тем же параметром `T`
- `Result` должен реализовать все чисто виртуальные методы `IResult<T>`
```cpp
template <typename T>
class Result final : public IResult<T>
```
Это типичный паттерн для реализации типа Result/Optional/Expected в C++, где:
- Используется шаблон для поддержки любых типов
- Применяется `final` для запрета наследования (часто для value-типов)
- Реализуется интерфейс для полиморфного использования
___
## Тип данных QByteArray
#QByteArray
```cpp
QByteArray localData = expressionResult.toLocal8Bit();
//конструктор который принимает указатель и размер
const std::string_view stdStr(localData.constData(), localData.size());
```
___
## **CRTP - Curiously Recurring Template Pattern**
Про шаблоны и где лучше применять?
#Статический_полиморфизм
```cpp
template <typename Derived>
class Serializable {
public:
    void serialize() {
        static_cast<Derived*>(this)->serializeImpl();
    }
    
    QString toJson() {
        return static_cast<const Derived*>(this)->toJsonImpl();
    }
};

class User : public Serializable<User> {
    friend class Serializable<User>;  // Дружественный для доступа к private
    
private:
    // Реализация для CRTP
    void serializeImpl() {
        qDebug() << "Serializing User...";
    }
    
    QString toJsonImpl() const {
        return "{ \"type\": \"user\" }";
    }
    
public:
    // Публичный интерфейс наследуется из Serializable
};

// Использование
User user;
user.serialize();  // Вызывает serializeImpl через CRTP
qDebug() << user.toJson();
```
___
# QString
#unicode
Проблема в том, что вы используете `QString::fromLocal8Bit()`, который предполагает, что строки в кодировке Local8Bit (обычно Windows-1251 в русских Windows). Однако:

1. **В вашем коде строки в UTF-8**, так как они написаны прямо в исходном коде на C++.
2. `QString::fromLocal8Bit()` интерпретирует байты как Local8Bit, что приводит к некорректному отображению русских символов.

**Решение:** Используйте `QString::fromUtf8()` или просто `QString::fromStdString()` с правильной кодировкой:
Дополнительные рекомендации:
1. **Проверьте кодировку исходного файла** (.cpp/.h) - она должна быть UTF-8.
2. **Проверьте, что возвращают методы GetCode(), GetDescription(), GetName()** - если они возвращают `std::string`, используйте `fromStdString()`.
___
# **Рекомендация:** 
#qpair #struct #tuple
Если вам нужно возвращать именно два значения, `QPair` - нормальный выбор. Но для лучшей читаемости кода используйте:
1. **Структуру** - если это бизнес-логика
2. **std::tuple** - если нужно вернуть гетерогенные данные
3. **QPair** - для простых случаев (как у вас)
___
# continue в циклах
#continue #for
```cpp
// 2. Если НЕ число
if (!isNumber)  
{
    qDebug() << "Пропуск: не число";
    continue;  // ← 3. ПРОПУСКАЕМ всю оставшуюся часть цикла для этой строки!
}

// 4. Сюда попадаем ТОЛЬКО если isNumber = true
auto prop = std::make_shared<mcc::Property>(...);  // ← выполнится ТОЛЬКО для чисел
mcc::PropertyValue propValue(prop, value);         // ← выполнится ТОЛЬКО для чисел
propertyValue.insert(...);
```
___
# Parsing или?
```cpp
// Шаг 1: Создаем копию строки
QString expr = expressionResult;  // "{equipProp1} + {equipProp2}"

// Шаг 2: Начинаем поиск с начала
int pos = 0;

// Шаг 3: Цикл пока находим '{'
while ((pos = expr.indexOf('{', pos)) != -1)
{
    // Шаг 4: Ищем парную '}'
    int end = expr.indexOf('}', pos);
    
    // Шаг 5: Если нет закрывающей - выходим
    if (end == -1) break;
    
    // Шаг 6: Извлекаем текст между скобками
    // mid(start, length) - берет подстроку
    QString var = expr.mid(pos + 1, end - pos - 1).trimmed();
    // Для "{equipProp1}": pos=0, end=11
    // mid(1, 10) → "equipProp1"
    
    // Шаг 7: Проверяем в propertyValue
    // find() ищет ключ в map, если не находит - возвращает end()
    if (propertyValue.find(var.toStdString()) == propertyValue.end())
    {
        // Шаг 8: Ошибка если переменная не найдена
        QMessageBox::warning(this, "Ошибка", 
            QString("Переменная '%1' не заполнена в таблице").arg(var));
        return; // Выходим из функции
    }
    
    // Шаг 9: Переходим к следующей скобке
    // Устанавливаем pos после текущей закрывающей скобки
    pos = end + 1; // Для продолжения поиска
}
```
___
# QVariant в параметрах
#improvement
Это позволит передавать дополнительные данные между окнами.
```cpp
class EditExpressionWindow : public QDialog
{
    Q_OBJECT
public:
    // Добавляем возможность передавать контекст
    EditExpressionWindow(const QString& title, 
                         const QVariant& context = QVariant(),
                         QWidget* parent = nullptr);
    
    void setContext(const QVariant& context);
    QVariant context() const;
    
signals:
    void expressionResultReady(const QString& result, const QVariant& context);
    
private:
    QVariant m_context;
};
```
___
# ошибка
#qt_bug
если в конструкторе объекта qwidget вместо родителя добавить другой виджет то он в нем и будет отрисован, ошибочно подставите QLabel то не определенное поведение будет родитель должен быть окном!
___
# ui(new UI::) зачем инициализируем через
```cpp
#include "stdafx.h"
#include "NipoletUI.h"

NipoletUI::NipoletUI(Element* el, DevicesDB* db, int workTemp, bool isPrototype, QWidget* parent) 
	: QBaseElementUI(parent, db), ui(new Ui::Nipolet) <-- Что создаем???
{
	ui->setupUi(this); <--- здесь мы устанавливаем форму
	curEl = el;
```
## UI можно удалить? delete ui->rkWelding_groupBox;
___
## static методы класса
#static #method #class
### **Объявление vs Определение**
- **Объявление** (в .h файле): `static Operation HighPressure;`
    - Говорит компилятору: "Такой объект где-то существует"
    - Не выделяет память
- **Определение** (в .cpp файле): `Operation Operation::HighPressure;`
    - **Выделяет память** для переменной
    - Создает реальный объект в памяти

### 2. **Как работает компиляция и линковка**
```text
Шаг 1: Компиляция каждого .cpp файла
- main.cpp видит: "static Operation HighPressure" ← только объявление
- Operation.cpp: может содержать или не содержать определение
Шаг 2: Линковка (сборка всех .obj файлов)
- main.obj ищет: "Где же находится Operation::HighPressure?"
- Если в Operation.obj нет определения → ОШИБКА LNK2001!
```

```cpp
## Реальный пример:

**Operation.h:**

cpp

class Operation {
private:
    std::string name;
public:
    Operation(const std::string& n) : name(n) {}
    
    // Объявления статических членов
    static Operation HighPressure;
    static Operation BoardCar;
    
    std::string getName() const { return name; }
};

**Operation.cpp:**

cpp

#include "Operation.h"
// ОПРЕДЕЛЕНИЯ (выделение памяти + инициализация)
Operation Operation::HighPressure("Высокое давление");
Operation Operation::BoardCar("Бортовой автомобиль");
// Без этих строк - HighPressure и BoardCar НЕ СУЩЕСТВУЮТ в памяти!

**main.cpp:**

cpp

#include "Operation.h"
int main() {
    // Используем статические переменные
    Operation op1 = Operation::HighPressure;  // Работает!
    Operation op2 = Operation::BoardCar;      // Работает!
    
    return 0;
}
```
## Интересный факт:
**Шаблонные статические переменные** (template) не требуют определения в .cpp файле, если они используются - они инстанцируются автоматически:
```cpp
template<typename T>
class MyClass {
public:
    static T value;  // Для шаблонов - определение обычно в .h файле
};
// Определение шаблонной статической переменной
template<typename T>
T MyClass<T>::value = T();  // В .h файле!
```
## Вывод:
**Статические переменные класса должны быть определены вне класса**, потому что:
1. Они существуют **вне объектов** класса
2. Нужно **выделить конкретную память** для них
3. Компилятору нужно знать **конкретный адрес** в памяти
4. Каждый модуль (файл .cpp) компилируется отдельно, и линковщик должен "склеить" все ссылки

**Без определения = нет памяти = нет объекта = ошибка линковки!**
___
#std_priority_queue #сортировка
`std::priority_queue` по умолчанию сортирует элементы по убыванию (самый большой наверху). Когда мы кладем пару `{value, key}`, сравнение происходит сначала по `first` (частота), и только если они равны - по `second` (значение).
___