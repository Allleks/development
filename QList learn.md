### Models
* Qt models tree и прочее как выделять ячейку(QModelIndex и производить операции с ней)
```cpp
QPair<QModelIndex, AbstractOperationsTreeViewItem*> OperationsWindow::getCurrentOperationsTreeItem(const QItemSelection& selectedItems) const
{
	try
	{
		auto selectionModel = operationsTreeView.selectionModel();
		if (selectionModel == nullptr)
{
	qCritical() << "SELECTIONMODEL IS NULL!";

	return QPair<QModelIndex, AbstractOperationsTreeViewItem*>();
	return QPair<QModelIndex, AbstractOperationsTreeViewItem*>(
	selectedIndexes.first(), 
	static_cast<AbstractOperationsTreeViewItem*>(selectedIndexes.first().internalPointer()));
}
```
Что означает Qt::UserRole? и как его использовать
```cpp
auto currentComponentId = componentVariations->at(item->data(Qt::UserRole).toInt())->GetComponentType()->GetId();
```
___
### Работа с событиями в qt 
```cpp
void closeEvent(QCloseEvent* event) override;

event->ignore();
event->accept();

emit onClose(true/false);
```
___
### QObjec когда используется? такая запись
```cpp
QObject::connect(
```
___
### Совет от бывалых про сигналы и слоты
Соединение всех слотов и сигналов надо выполнять в **главном окне,** так как именно оно знает всё о формах, которые в нём используются.
___
# Реализовать сериализацию/десериализацию данных ????
___
**QStrintList** - 
```cpp
QStringList headers = {"Код", "Название", "Описание", "Ед.изм", "Мин. знач", "Макс.знач", "Тест. знач"};
```
___
### **Слоты без параметров** - напрямую
```cpp
// Если метод объявлен как слот и не принимает параметров
connect(button, &QPushButton::clicked, this, &MyClass::someSlot);
```
### **Методы с параметрами или обычные функции** - через лямбду
```cpp
// Best practice для вашего случая
connect(ui.platformHeightExpressionEdit_button, &QPushButton::clicked,
        this, [this]() {
    // Лямбда захватывает this и может обращаться к UI
    QString text = ui.platformHeightExpression_label->text();
    showEditExpressionWindow(text);  // Вызов метода с параметром
});
```
**Вывод:** Для вызова методов с параметрами или любой сложной логики обработки используйте лямбды - это современный и рекомендуемый подход в Qt.
___
## sourcegit 
не смог сделать слияние в старую  ветку(ошибка), решил вопрос через bash консоль git так все ок
___
## Работаем с выделенным содержимым ячеек таблицы
```cpp
//метод selected проходимся по выделенным ячейкам
for (const auto item : ui.componentVariation_table->selectedItems())
{
//проверка если столбец есть идем дальше
	if (item->column() != 0)
	{
		continue;
	}
	//вытаскиваем содержимое ячейки черех data и роль переводим в нужный тип
	auto componentVariationId = item->data(Qt::UserRole).toInt();
	//и дальше логика с полученными данными	
}
```
___
## В VisualStudio подключаем хедер ui формы
```cpp
//смотрим OutpulFile в настройках проекта
form.ui -> #include "ui_form_window.h"
```
___
## Модальность окна в ui design
3 вида модальности, на проекте 3 вариант use
```cpp
windowModality
{
	NonModal,
	WindowModal,
	ApplicationModal  <-----
}
```
___
## QTextEdit
возврат содержимого
```cpp
QString inputExpressionText = ui.expressionInput_textEdit->toPlainText();
```
___
## Про типы данных QString и 
- `QString` хранит данные в UTF-16 (в Windows), а `std::string_view` ожидает UTF-8
- Нужно явное преобразование через `toUtf8()` или `toStdString()`
___
## Механизм поиска
```cpp
connect(ui.searchSparePart_lineEdit, &QLineEdit::editingFinished, this, &HardcodedSparePartWindow::findSpareParts);
```
___
### **Особенности видимости и объявление сигналов/слотов**
Сигналы и слоты в Qt имеют глобальную видимость в пределах одного `QObject` дерева. Они не требуют соединения только в главном окне
```cpp
// Окно 1 (FormA.cpp) - может соединить свой сигнал со слотом Окна 2
connect(this, &FormA::mySignal, formB, &FormB::handleSignal);

// Окно 2 (FormB.cpp) - может соединить свой сигнал со слотом Окна 3
connect(this, &FormB::anotherSignal, formC, &FormC::processData);
```
**Вывод:** Соединения можно и нужно делать в наиболее подходящем месте с точки зрения архитектуры, а не только в главном окне.
**Ключевой принцип:** Соединяйте объекты в том месте, где у вас есть естественный доступ к обоим участникам соединения. Часто это место создания объектов или инициализации модуля.
## **📊 ТАБЛИЦА ВОЗМОЖНОСТЕЙ:**

|Где создаем connect|Доступ к источнику|Доступ к приемнику|Пример|
|---|---|---|---|
|В классе-источнике|✅ `this`|✅ Через параметр|`connect(this, &A::sig, target, &B::slot)`|
|В классе-приемнике|✅ Через параметр|✅ `this`|`connect(source, &A::sig, this, &B::slot)`|
|В родительском классе|✅ Члены класса|✅ Члены класса|В `MainWindow` соединяет своих детей|
|В отдельном сервисе|✅ Переданы как параметры|✅ Переданы как параметры|`Service::connect(a, b)`|
|Глобально|✅ Через синглтон|✅ Через синглтон|`Registry::connectAll()`|

## **🚨 ЧЕГО НЕ ДЕЛАТЬ:**
```cpp
// ❌ ПЛОХО: Соединения разбросаны по всему коду
void func1() { connect(a, b); }
void func2() { connect(c, d); }

// ❌ ОПАСНО: Нет проверки существования объектов
connect(pointerThatMayBeNull, &Class::signal, receiver, &Class::slot);

// ✅ ХОРОШО: Централизованное управление соединениями
class ConnectionManager {
    void setupAllConnections() {
        connect(a, b);
        connect(c, d);
        connect(e, f);
    }
};
```