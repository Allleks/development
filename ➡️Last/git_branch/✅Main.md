### **. Функциональность (новые возможности)**
- **`feat/`** — новая фича/функция (самый частый)
    - `feat/user-registration`
    - `feat/search-filter`
- **`feature/`** — то же самое, но длиннее (реже)
___
### **2. Исправления ошибок**
- **`fix/`** — исправление бага
    - `fix/login-crash`
    - `fix/memory-leak`
- **`bugfix/`** — явное указание на исправление бага
- **`hotfix/`** — срочное исправление в production
___
### **3. Верстка и интерфейс**
- **`ui/`** — создание/верстка UI компонентов ✅ ваш случай
    - `ui/settings-modal`
    - `ui/navbar`
- **`layout/`** — работа над макетом, сетками
- **`style/`** — стили, CSS, анимации
    - `style/dark-theme`
    - `style/button-hover`
___
### **4. Документация**
- **`docs/`** — только документация, без кода
    - `docs/api-reference`
    - `docs/installation-guide`
    - `docs/readme-update`
- **`readme/`** — только README файл
___
### **5. Рефакторинг**
- **`refactor/`** — улучшение кода без изменения функциональности
    - `refactor/auth-service`
    - `refactor/cleanup-legacy-code`
- **`chore/`** — мелкие технические правки
    - `chore/update-dependencies`
    - `chore/ci-config`
___
## 🛠 **Технические/специфичные:**

### **6. Тестирование**
- **`test/`** — добавление/исправление тестов
    - `test/login-validation`
    - `test/unit-coverage`
- **`tests/`** — альтернативный вариант
___
### **7. Инфраструктура и настройки**
- **`config/`** — конфигурационные файлы
    - `config/nginx-setup`
    - `config/env-vars`
- **`infra/`** — инфраструктура (Docker, сервера)
- **`ci/`** или **`cd/`** — настройка CI/CD
    - `ci/github-actions`
    - `cd/deployment-script`
___
### **8. Производительность**
- **`perf/`** — оптимизация производительности
    - `perf/database-query`
    - `perf/image-optimization`
### **9. Безопасность**
- **`security/`** — исправления уязвимостей
    - `security/xss-fix`
    - `security/auth-tokens`
___
## 📝 **Специальные префиксы:**

### **10. Эксперименты и черновики**
- **`experiment/`** — пробуем новую технологию
    - `experiment/redis-cache`
- **`spike/`** — исследовательская задача
    - `spike/migration-approach`
- **`draft/`** — черновик, не для мержа
- **`wip/`** — работа в процессе (можно мержить по частям)
    - `wip/payment-integration`
___
### **11. Версии и релизы**
- **`release/`** — подготовка к релизу
    - `release/v1.2.0`
- **`version/`** — управление версиями
___
## 🎯 **Примеры из реальной жизни:**
# Типичный рабочий день:
```bash
git checkout -b ui/expression-window      # Верстаю окно
git checkout -b feat/expression-logic     # Добавляю логику
git checkout -b fix/expression-validation # Исправляю баг
git checkout -b test/expression-component # Пишу тесты
git checkout -b docs/expression-api       # Документирую
git checkout -b refactor/expression-code  # Улучшаю код
```
___
## 📊 **Когда что использовать:**
```text
Если меняете:              → Используйте:
-------------------        → -------------
Внешний вид/верстку       → ui/, style/
Логику/функциональность   → feat/, feature/
Исправляете ошибку        → fix/, bugfix/
Документацию              → docs/
Тесты                     → test/
Настройки/конфиги         → config/, chore/
Производительность        → perf/
Безопасность              → security/
```
