```cpp
┌─────────────────────────────────────────────────────┐
│  Способ А (опасно):                                 │
│  "SELECT * FROM Проемы WHERE id = " + userInput     │
│                                                     │
│  Если userInput = "1; DROP TABLE Проемы; --"        │
│  Запрос станет:                                     │
│  SELECT * FROM Проемы WHERE id = 1;                 │
│  DROP TABLE Проемы; --                              │
│                                                     │
│  Результат: Таблица УДАЛЕНА! 💀                     │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────-────┐
│  Способ Б (безопасно):                               │
│  query.prepare("SELECT * FROM Проемы WHERE id = :id")│
│  query.bindValue(":id", userInput)                   │
│                                                      │
│  Если userInput = "1; DROP TABLE Проемы; --"         │
│  База данных воспримет это как ОДНО ЗНАЧЕНИЕ:        │
│  "1; DROP TABLE Проемы; --"                          │
│  И будет искать id = такому странному числу          │
│                                                      │
│  Результат: Ничего не найдет, но таблица ЦЕЛА! ✅    │
└───────────────────────────────────────────────────-──┘
```