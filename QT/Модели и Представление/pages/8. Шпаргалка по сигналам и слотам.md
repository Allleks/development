```cpp
// Подписка на изменения модели:
connect(model, &QAbstractItemModel::dataChanged, 
        view, &QAbstractItemView::update);

// Реакция на выбор:
connect(view->selectionModel(), &QItemSelectionModel::selectionChanged,
        this, &MyClass::onSelectionChanged);
//Двойной клик
connect(&operationsTreeView,&QTreeView::doubleClicked, this, &OperationsWindow::editSelectedWithDoubleClick);

// Контекстное меню:
view->setContextMenuPolicy(Qt::CustomContextMenu);
connect(view, &QWidget::customContextMenuRequested,
        this, &MyClass::showContextMenu);

// Drag & Drop:
view->setDragEnabled(true);
view->setAcceptDrops(true);
view->setDropIndicatorShown(true);
```