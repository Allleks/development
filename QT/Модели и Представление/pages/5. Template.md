```cpp
class ProductionTableModel : public QAbstractTableModel
{
    Q_OBJECT

public:
    explicit ProductionTableModel(QObject *parent = nullptr);
    
    // Обязательные методы
    int rowCount(const QModelIndex &parent = QModelIndex()) const override;
    int columnCount(const QModelIndex &parent = QModelIndex()) const override;
    QVariant data(const QModelIndex &index, int role = Qt::DisplayRole) const override;
    QVariant headerData(int section, Qt::Orientation orientation,
                       int role = Qt::DisplayRole) const override;
    
    // Для редактирования
    bool setData(const QModelIndex &index, const QVariant &value,
                int role = Qt::EditRole) override;
    Qt::ItemFlags flags(const QModelIndex &index) const override;
    
    // CRUD операции с правильными сигналами
    bool insertRow(int row, const QVariantMap &data);
    bool removeRow(int row);
    bool updateRow(int row, const QVariantMap &data);
    
    // Пагинация и фильтрация
    void setFilter(const QString &filter);
    void setPage(int page, int itemsPerPage = 50);
    
    // Оптимизация
    bool canFetchMore(const QModelIndex &parent) const override;
    void fetchMore(const QModelIndex &parent) override;

private:
    // Приватные методы для работы с данными
    void loadData();
    void emitDataChanged(int row);
    
    // Кеширование для производительности
    mutable QCache<QModelIndex, QVariant> m_dataCache;
    
    // Данные
    QVector<DataItem> m_data;
    QString m_filter;
    int m_currentPage = 0;
    int m_itemsPerPage = 50;
    bool m_hasMoreData = true;
};
```