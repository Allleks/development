также есть метод для получения типа
```cpp
		case Qt::ToolTipRole:
		{
			const std::type_info& type = typeid(*this);

			if (strstr(type.name(), "OperationsTreeViewModel") != nullptr)
			{
				if (index.column() == 1 || index.column() == 2)
				{
					return item->data(index.column());
				}
			}
		}
```