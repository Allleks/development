
```cpp
   beginRemoveRows(index.parent(), row, row);
        
        // 4. Удаляем из данных модели
        parentItem->deleteChild(row); // Ваш метод удаления
        
        // 5. Уведомляем представление КОНЕЦ удаления
        endRemoveRows();
```