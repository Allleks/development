[QShortcut Class | Qt GUI | Qt 6.10.1](https://doc.qt.io/qt-6/qshortcut.html)
Просто в конструкnор как один из вариантов
```cpp
// Создаем шорткат для Escape
QShortcut* escapeShortcut = new QShortcut(QKeySequence(Qt::Key_Escape), this);
//и лямбдой например делаем какоето действие
connect(escapeShortcut, &QShortcut::activated, this, [this]() {this->close();});
```