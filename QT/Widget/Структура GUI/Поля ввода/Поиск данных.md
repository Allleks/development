```cpp
 connect(ui.searchSparePart_lineEdit, &QLineEdit::editingFinished, this, &HardcodedSparePartWindow::findSpareParts);
 
 
 void HardcodedSparePartWindow::findSpareParts()
{
    try
    {
        auto result = sparePartRepository->GetSpareParts(nullptr);

        if (!result.IsSuccess())
        {
            QMessageBox::critical(this, "Поиск МТР", "Не удалось найти МТР в БД! См. лог");

            qCritical() << "UNABLE TO LOAD SPARE PARTS";

            return;
        }

        foundSpareParts = result.TakeValue();

        ui.spareParts_table->clearContents();

        fillSparePartsTable();
    }
    catch (const std::exception& ex)
    {
        qCritical() << ex.what();
    
```