**Ваш `IRepositoriesManager` в контексте паттернов:**
1. **Это Unit of Work** (как указано в комментарии) - группировка операций с БД
2. **Это Фабрика** - создаёт репозитории
3. **Это Стратегия** - можно подменить реализацию (SQLite/PostgreSQL)