Виды *конструктивного элемента* из метода `GetComponentType()->GetName()`
```cpp
ui.componentVariation_table->setItem(currentRow, 1, new QTableWidgetItem(QString::fromLocal8Bit(componentVariation->GetComponentType()->GetName())));
```